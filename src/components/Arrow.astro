---
interface Props { diagonal?: boolean; }
const { diagonal = false } = Astro.props;
---
<svg class="arrow" width="22" height="22" viewBox="0 0 24 24" fill="none" aria-hidden="true" style={diagonal ? 'transform:rotate(-45deg)' : undefined}><path d="M4 12h15m-6-6 6 6-6 6" stroke="currentColor" stroke-width="1.4"/></svg>
